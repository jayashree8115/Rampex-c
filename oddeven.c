#include<stdio.h>
int main()
{
    int a,num;
    printf("enter number:",num);
    scanf("%d",&num);
    if(num%2==0)
    {
    printf("a is even %d",num);
    }
    else
    {
        printf("a is odd %d",num);
    }
}